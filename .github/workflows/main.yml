name: Test
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - uses: actions/setup-node@v1.1.0
      with:
        node-version: '10.x'
    
    - run: npm install
    - run: npm test
